name: Visual compare

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  visual-compare:
    name: Visual comparison on PR
    runs-on: ubuntu-latest

    steps:
      - name: Checkout PR head
        uses: actions/checkout@v5
        with:
          ref: ${{ github.head_ref }}

      - name: Checkout base (target branch)
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          path: baseline-ref

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '24'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Verify Playwright installation
        run: |
          echo "Playwright CLI version:"
          npx playwright --version || true
          echo "Node require check for Playwright modules:"
          node -e "try{require('playwright'); console.log('playwright: ok')}catch(e){console.log('playwright: not available')}"
          node -e "try{require('playwright-core'); console.log('playwright-core: ok')}catch(e){console.log('playwright-core: not available')}"
          node -e "try{require('@playwright/test'); console.log('@playwright/test: ok')}catch(e){console.log('@playwright/test: not available')}"
          echo "List Playwright cache (if present):"
          ls -la ~/.cache/ms-playwright || true
        shell: bash

      - name: Run visual comparison
        id: compare
        run: |
          set +e
          mkdir -p e2e/baseline-generated
          node scripts/compare-baseline.js 2>&1 | tee e2e/baseline-generated/compare.log
          rc=${PIPESTATUS[0]}
          set -e
          echo "compare_exit_code=$rc" >> $GITHUB_OUTPUT
        continue-on-error: true
        env:
          BASELINE_REF_DIR: baseline-ref/e2e/baseline
          GENERATED_DIR: e2e/baseline-generated
          PIXELMATCH_THRESHOLD: '0.02'

      - name: Upload generated artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: visual-compare-output
          path: e2e/baseline-generated/**

      - name: Comment PR with visual-compare result
        if: always()
        uses: peter-evans/create-or-update-comment@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            Visual comparison completed for this PR.

            - Compare exit code: ${{ steps.compare.outputs.compare_exit_code }}
            - Artifacts: `visual-compare-output` (download from workflow run)
            - Run details: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}

            If the compare exit code is `2`, visual differences exceeded the configured threshold. Please download the artifacts to inspect diffs and generated images.

