{
  "name": "naukazmichalem",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "test:visual": "npx playwright test tests/visual.spec.ts",
    "test:content": "npx playwright test tests/content.spec.ts",
    "test:report": "npx playwright test --reporter=html",
    "test:headed": "npx playwright test --headed",
    "test:ui": "npx playwright test --ui",
    "generate:favicons": "node scripts/generate-favicons.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.43.0",
    "@types/node": "^20.14.6",
    "png-to-ico": "^2.0.0",
    "sharp": "^0.33.0",
    "typescript": "^5.5.4"
  }
}
