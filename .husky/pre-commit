
#!/bin/sh
# Husky hook (forward-compatible): avoid sourcing internal husky.sh to prevent
# deprecation warnings for future Husky releases. Run lint-staged on staged files.
npx --no-install lint-staged

